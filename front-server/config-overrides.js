// const { override } = require('customize-cra');

// // 웹뷰 앱에서 주소창을 숨기는 설정
// const hideAddressBar = (config) => {
//   config.target = 'webview'; // 웹뷰 앱의 target 설정
//   config.mobile = true; // 모바일 기기에서 실행될 경우에만 적용
//   return config;
// };

// module.exports = override(hideAddressBar);